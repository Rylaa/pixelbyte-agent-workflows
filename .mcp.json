{
  "mcpServers": {
    "pixelbyte-figma-mcp": {
      "command": "uvx",
      "args": ["pixelbyte-figma-mcp"],
      "env": {
        "FIGMA_PERSONAL_ACCESS_TOKEN": "${FIGMA_PERSONAL_ACCESS_TOKEN}"
      }
    }
  }
}
